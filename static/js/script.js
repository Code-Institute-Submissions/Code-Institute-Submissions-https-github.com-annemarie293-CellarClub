$(document).ready(function(){
    //Jquery for Mobile navbar collapse (Materialize)
    $('.sidenav').sidenav();
    //Jquery for Modal on Wines page (Materialize)
    $('.modal').modal();
    //Jquery for selet option on forms (Materialize)
    $('select').formSelect();
  });